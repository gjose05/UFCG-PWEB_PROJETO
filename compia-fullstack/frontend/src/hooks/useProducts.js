import {useEffect,useState} from 'react'; import {productsApi} from '../api/products'; export function useProducts(filters={}){const[data,setData]=useState([]),[loading,setLoading]=useState(true),[error,setError]=useState(''),key=JSON.stringify(filters); useEffect(()=>{let a=true;setLoading(true);productsApi.list(filters).then(x=>a&&setData(x)).catch(e=>a&&setError(e.message)).finally(()=>a&&setLoading(false));return()=>{a=false}},[key]);return{products:data,loading,error}}
