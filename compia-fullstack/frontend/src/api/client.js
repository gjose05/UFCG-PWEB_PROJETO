const BASE=import.meta.env.VITE_API_URL||'http://localhost:8000/api/v1';
export async function api(path,options={}){const h=new Headers(options.headers); if(options.body)h.set('Content-Type','application/json'); const t=localStorage.getItem('compia_token'); if(t)h.set('Authorization',`Bearer ${t}`); const r=await fetch(BASE+path,{...options,headers:h}); if(!r.ok){let m='Não foi possível concluir a operação.'; try{const j=await r.json(); if(typeof j.detail==='string')m=j.detail}catch{} if(r.status===401)localStorage.removeItem('compia_token'); throw new Error(m)} return r.status===204?null:r.json()}
