import {Link} from 'react-router-dom'; import {useCart} from '../../hooks/useCart'; import {money} from '../../utils/money';
const label={physical:'Livro físico',digital:'E-book',kit:'Kit'};
export function ProductCard({product}){const{add}=useCart();return <article className="card product-card"><Link className="product-art" to={`/produtos/${product.id}`}>{product.cover_image_url?<img className="cover-image" src={product.cover_image_url} alt={product.title}/>:<div className={`mini-cover ${product.cover_color}`}><small>COMPIA</small><strong>{product.title}</strong><small>{label[product.product_type]}</small></div>}</Link><div className="product-body"><span className="product-category">{product.category}</span><Link className="product-title" to={`/produtos/${product.id}`}>{product.title}</Link><span className="product-meta">{product.author} • {label[product.product_type]}</span><div className="product-bottom"><span className="price">{money(product.price)} {product.old_price&&<small className="old-price">{money(product.old_price)}</small>}</span><button className="btn btn-primary btn-small" onClick={()=>add(product)}>Adicionar</button></div></div></article>}
