import {createContext,useEffect,useState} from 'react'; import {authApi} from '../api/auth'; export const AuthContext=createContext(null);
export function AuthProvider({children}){const[user,setUser]=useState(null),[loading,setLoading]=useState(true); useEffect(()=>{if(!localStorage.getItem('compia_token'))return setLoading(false); authApi.me().then(setUser).catch(()=>localStorage.removeItem('compia_token')).finally(()=>setLoading(false))},[]); async function login(e,p){const r=await authApi.login(e,p);localStorage.setItem('compia_token',r.access_token);setUser(r.user)} async function register(n,e,p){const r=await authApi.register(n,e,p);localStorage.setItem('compia_token',r.access_token);setUser(r.user)} function logout(){localStorage.removeItem('compia_token');setUser(null)} return <AuthContext.Provider value={{user,loading,login,register,logout}}>{children}</AuthContext.Provider>}
