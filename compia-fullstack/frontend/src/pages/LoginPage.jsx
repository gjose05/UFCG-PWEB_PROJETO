import {useState} from 'react'; import {Link,useNavigate,Navigate} from 'react-router-dom'; import {useAuth} from '../hooks/useAuth'; import {ErrorMessage} from '../components/ui/ErrorMessage';
export function LoginPage(){const{user,login}=useAuth(),nav=useNavigate(),[email,setEmail]=useState(''),[password,setPassword]=useState(''),[error,setError]=useState('');if(user)return <Navigate to="/conta"/>;async function submit(e){e.preventDefault();try{await login(email,password);nav('/conta')}catch(x){setError(x.message)}}return <main className="auth-wrap"><form className="card auth-card stack" onSubmit={submit}><div><h1>Entrar</h1><p>Acesse sua conta para acompanhar pedidos.</p></div>{error&&<ErrorMessage message={error}/>}<div className="field"><label>E-mail</label><input className="input" type="email" value={email} onChange={e=>setEmail(e.target.value)} required/></div><div className="field"><label>Senha</label><input className="input" type="password" value={password} onChange={e=>setPassword(e.target.value)} required/></div><button className="btn btn-primary">Entrar</button><p className="helper center">Não tem conta? <Link className="text-link" to="/cadastro">Cadastre-se</Link></p></form></main>}
