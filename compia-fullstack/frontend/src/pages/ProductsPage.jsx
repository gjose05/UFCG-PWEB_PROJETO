import {useState} from 'react'; import {useProducts} from '../hooks/useProducts'; import {ProductCard} from '../components/product/ProductCard'; import {Loading} from '../components/ui/Loading'; import {ErrorMessage} from '../components/ui/ErrorMessage';
export function ProductsPage(){const[search,setSearch]=useState(''),[category,setCategory]=useState(''),[order,setOrder]=useState('');const{products,loading,error}=useProducts({search,category,order});return <main><div className="container page-head"><h1>Catálogo</h1><p>Encontre livros físicos, e-books e kits sobre tecnologia.</p></div><section className="container section compact-top"><div className="filters"><input className="input" value={search} onChange={e=>setSearch(e.target.value)} placeholder="Buscar título, autor ou tema..."/><select className="select" value={category} onChange={e=>setCategory(e.target.value)}><option value="">Todas as categorias</option><option>Inteligência Artificial</option><option>Arquitetura</option><option>Blockchain</option><option>Cibersegurança</option><option>Kits</option></select><select className="select" value={order} onChange={e=>setOrder(e.target.value)}><option value="">Ordenar</option><option value="price-asc">Menor preço</option><option value="price-desc">Maior preço</option></select></div>{error&&<ErrorMessage message={error}/>} {loading?<Loading/>:<div className="grid grid-3">{products.map(p=><ProductCard key={p.id} product={p}/>)}</div>}</section></main>}
