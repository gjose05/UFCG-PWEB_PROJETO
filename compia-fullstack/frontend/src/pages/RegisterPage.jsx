import {useState} from 'react'; import {Link,useNavigate,Navigate} from 'react-router-dom'; import {useAuth} from '../hooks/useAuth'; import {ErrorMessage} from '../components/ui/ErrorMessage';
export function RegisterPage(){const{user,register}=useAuth(),nav=useNavigate(),[name,setName]=useState(''),[email,setEmail]=useState(''),[password,setPassword]=useState(''),[error,setError]=useState('');if(user)return <Navigate to="/conta"/>;async function submit(e){e.preventDefault();try{await register(name,email,password);nav('/conta')}catch(x){setError(x.message)}}return <main className="auth-wrap"><form className="card auth-card stack" onSubmit={submit}><div><h1>Criar conta</h1><p>Cadastre-se para comprar e acompanhar pedidos.</p></div>{error&&<ErrorMessage message={error}/>}<div className="field"><label>Nome completo</label><input className="input" value={name} onChange={e=>setName(e.target.value)} required/></div><div className="field"><label>E-mail</label><input className="input" type="email" value={email} onChange={e=>setEmail(e.target.value)} required/></div><div className="field"><label>Senha</label><input className="input" type="password" minLength="8" value={password} onChange={e=>setPassword(e.target.value)} required/></div><button className="btn btn-primary">Cadastrar</button><p className="helper center">Já tem conta? <Link className="text-link" to="/login">Entrar</Link></p></form></main>}
